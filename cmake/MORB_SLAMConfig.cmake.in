get_filename_component(MORB_SLAM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${MORB_SLAM_CMAKE_DIR})
list(REMOVE_AT CMAKE_MODULE_PATH -1)

find_dependency(boost_regex @MIN_BOOST_VERSION@)

if(NOT TARGET MORB_SLAM::MORB_SLAM)
    include("/usr/local/share/MORB_SLAM/cmake/MORB_SLAM/MORB_SLAMTargets.cmake")
endif()

set(MORB_SLAM_LIBRARIES MORB_SLAM::MORB_SLAM)